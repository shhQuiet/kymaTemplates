> Historical development record. Current architecture and status: /Users/stevehorne/dev/kyma/CURRENT.md. Later findings supersede earlier hypotheses below.

# Patch-specific outer Script: brightness random walk

Status: prepared from documented Script nesting and SoundToGlobalController; not yet playback-tested. Keep the existing drone Script unchanged. Use it as input to a new outer Script.

Outer Inputs, in order: Initialize, Drone, Control. Drone is the existing inner Script renamed to an identifier-style name. Control is a SoundToGlobalController (not the triggered initializer) with GeneratedEvent ?event, Value ?value, Silent checked, ShowInVCS checked. Initialize is the verified generic triggered template. The drone's own Inputs stay Initialize, VCF, Oscillator, Level, Delay. Keep the single outer MIDIVoice after the new outer Script, Polyphony 1/Live MIDI/channel1; avoid adding a second MIDIVoice. Conventional keyboard needs MPE disabled.

Paste the complete build.st into the outer Script only. Set BrightnessLow and BrightnessHigh VCS ranges to 40-5000 Hz; startup values 120 and 700 are supplied by Initialize. Existing drone controls are initialized by the inner Script. The low/high bounds are sorted and limited to at least 40 Hz. Those bounds govern DroneBrightness (base cutoff); the inner DroneDepth sweep still adds to it, so they do not bound the final instantaneous cutoff.

Walk takes one step per second with stepSize 0.3, folds the native signed wrapping walk through abs into 0-1, smooths for 0.8 seconds, maps to the limits and clamps to them. This avoids the raw walk's wrap jumping from low to high. Changing limits may move brightness abruptly due to clamping; endpoint edits are not a promise of rate-limited motion.

The gated controller is intended to drive !DroneBrightness continuously; use the limits to steer it. Interaction between continuous writing and AllowLiveOverride remains to be tested. The inner one-shot brightness initializer still runs at startup, so a startup ordering transient remains possible; continuous ownership must be verified. The writer is intended to reach the inner Sound via global EventValue scope. Matching channels and any local event remapping affect this and must be tested.

Reference: Sound Class Reference printed 308 explicitly permits a Script inside another Script; 327 documents SoundToGlobalController global output, same-range values, and display-only VCS target. Capytalk Reference 230 documents the signed wrapping random walk and squared stepSize scaling. Generic ?event binding is verified for the triggered controller, not yet for this continuous class.

Test: play outer MIDIVoice, provide MIDI notes, watch DroneBrightness remain between low/high and wander. Set both limits equal (e.g. 300 Hz) to verify it holds that base cutoff. Then restore differing limits. No native .kym has been generated by the agent.

Current Control template: user chose an independent copy of Initialize (TriggeredSoundToGlobalController) with Gated checked and Trigger 1; ?event/?value and Silent remain configured. This supersedes the new SoundToGlobalController instruction above. Gated continuous operation remains to be tested.

Compilation correction: unparenthesized vmax: low vmin: high was parsed as nonexistent vmax:vmin:. build.st now computes brightness separately and uses (brightness vmax: low) vmin: high. User error was in this outer script, not evidence against Script nesting. Awaiting corrected playback.

Architecture: this outer Script is a patch-specific controller, just like the inner drone Script. It is not intended as a reusable prototype. The reusable continuous-control building block is InitializerGated (an independent copy of Initializer with Gated checked), whose creation and name are now confirmed by the user’s collection screenshot; internal settings and playback are not established by that screenshot.

## Shared-parent integration correction

User reports brightness walk lost after shared-parent insertion; cause not yet established. build-shared-system.st restores explicit outer scheduling and resolves inputs by exact names Initializer, KymaSystem, InitializerGated (any order). KymaSystem contains drone and passes findInput; drone keeps five templates. Chain drone -> KymaSystem -> brightness Script -> existing MIDI voice. The outer wrapper locally defines lookup because it is outside the shared parent scope. This minimal integration fix preserves the existing topology; do not claim the loss was definitively due to ordering. Awaiting playback.
